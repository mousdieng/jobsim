# ============================================
# AI PROVIDER CONFIGURATION
# Choose which AI provider to use: "openai", "claude", or "mock"
# ============================================
AI_PROVIDER=mock

# ============================================
# OPENAI CONFIGURATION (when AI_PROVIDER=openai)
# Get your key from: https://platform.openai.com/api-keys
# ============================================
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4-turbo-preview

# Alternative models for OpenAI:
# - gpt-4-turbo-preview (recommended for best quality)
# - gpt-4 (standard GPT-4)
# - gpt-3.5-turbo (faster, cheaper)

# ============================================
# ANTHROPIC (CLAUDE) CONFIGURATION (when AI_PROVIDER=claude)
# Get your key from: https://console.anthropic.com/
# ============================================
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Alternative models for Claude:
# - claude-3-5-sonnet-20241022 (recommended - best balance)
# - claude-3-opus-20240229 (highest quality, slower)
# - claude-3-sonnet-20240229 (balanced)
# - claude-3-haiku-20240307 (fastest, cheapest)

# ============================================
# MOCK PROVIDER (when AI_PROVIDER=mock)
# For testing without spending money - NO API KEY REQUIRED!
# ============================================
# When using mock provider:
# - No API calls are made
# - Returns realistic sample data instantly
# - Perfect for development and testing
# - No costs incurred
# - Supabase is optional (can test without it)

# ============================================
# SUPABASE CONFIGURATION (REQUIRED)
# Use SERVICE ROLE key, not anon key
# Get from: https://app.supabase.com/project/_/settings/api
# ============================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-here

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3001
NODE_ENV=development

# CORS Configuration (comma-separated origins)
CORS_ORIGIN=http://localhost:4200

# ============================================
# RATE LIMITING
# ============================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# AI ENGINE SETTINGS
# ============================================
MAX_TASKS_PER_GENERATION=5
MAX_MEETINGS_PER_TASK=3
DEFAULT_TASK_DEADLINE_DAYS=7

# ============================================
# USAGE NOTES
# ============================================
#
# To switch between providers, just change AI_PROVIDER:
# - Set AI_PROVIDER=mock to test WITHOUT spending money (recommended for dev)
# - Set AI_PROVIDER=openai to use OpenAI GPT models
# - Set AI_PROVIDER=claude to use Anthropic Claude models
#
# You only need the API key for the provider you're using.
# The system will validate this on startup.
#
# Cost comparison (approximate):
# Mock Provider: $0.00 (FREE - perfect for testing!)
# OpenAI GPT-4: ~$0.01-0.03 per 1K tokens
# Claude 3.5 Sonnet: ~$0.003-0.015 per 1K tokens
# Claude 3 Haiku: ~$0.00025-0.00125 per 1K tokens
#
# Recommendation for development:
# 1. Start with AI_PROVIDER=mock (free, instant responses)
# 2. Test your integration and workflows
# 3. Switch to real AI when ready for quality testing
#
